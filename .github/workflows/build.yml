name: Build Claudia

on:
  workflow_call:
    inputs:
      repository:
        description: Repository to clone and build
        type: string
        default: getAsterisk/claudia
      ref:
        description: Branch/tag/ref to build
        type: string
        required: true
  workflow_dispatch:
    inputs:
      repository:
        description: Repository to clone and build
        type: string
        default: getAsterisk/claudia
      ref:
        description: Branch/tag/ref to build
        type: string
        default: main

jobs:
  build:
    runs-on: windows-latest
    strategy:
      matrix:
        target: [x86_64-pc-windows-msvc]
        include:
          - target: x86_64-pc-windows-msvc
            artifact_name: claudia-windows-x64

    steps:
      - name: Enable long paths in Git
        run: |
          git config --system core.longpaths true

      - name: Enable long paths in Windows
        shell: powershell
        run: |
          New-ItemProperty -Path "HKLM:\SYSTEM\CurrentControlSet\Control\FileSystem" `
            -Name "LongPathsEnabled" -Value 1 -PropertyType DWORD -Force

      - name: Checkout self
        uses: actions/checkout@v4

      - name: Checkout Claudia
        uses: actions/checkout@v4
        with:
          repository: ${{ inputs.repository }}
          ref: ${{ inputs.ref }}
          path: claudia

      - name: Install WebView2
        shell: powershell
        run: |
          # Download and install WebView2 Runtime
          Invoke-WebRequest -Uri "https://go.microsoft.com/fwlink/p/?LinkId=2124703" -OutFile "MicrosoftEdgeWebview2Setup.exe"
          Start-Process -FilePath "MicrosoftEdgeWebview2Setup.exe" -ArgumentList "/silent", "/install" -Wait

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
          targets: ${{ matrix.target }}

      - name: Install Bun
        shell: powershell
        run: |
          irm bun.sh/install.ps1 | iex

      - name: Add Bun to PATH
        shell: powershell
        run: |
          $bunPath = "$env:USERPROFILE\.bun\bin"
          echo "$bunPath" >> $env:GITHUB_PATH

      - name: Rust Cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: "claudia/src-tauri -> target"

      - name: Install frontend dependencies
        working-directory: claudia
        shell: powershell
        run: |
          bun install

      - name: Fix icon format issue
        working-directory: claudia
        shell: powershell
        run: |
          # Check if the problematic icon exists and remove it to use Tauri's default
          if (Test-Path "src-tauri\icons\icon.ico") {
            Write-Host "Removing problematic icon.ico file..."
            Remove-Item "src-tauri\icons\icon.ico" -Force
          }
          
          # Also check for and remove other potentially problematic icon files
          if (Test-Path "src-tauri\icons") {
            Get-ChildItem "src-tauri\icons\*.ico" | ForEach-Object {
              Write-Host "Removing icon file: $($_.Name)"
              Remove-Item $_.FullName -Force
            }
          }
          
          # Let Tauri use its default icon or generate one
          Write-Host "Icon files removed. Tauri will use default icon."

      - name: Build Tauri application
        working-directory: claudia
        shell: powershell
        env:
          RUST_BACKTRACE: 1
        run: |
          bun run tauri build --target ${{ matrix.target }}

      - name: Archive executable
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}-exe
          path: claudia/src-tauri/target/${{ matrix.target }}/release/claudia.exe

      - name: Archive MSI installer
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}-msi
          path: claudia/src-tauri/target/${{ matrix.target }}/release/bundle/msi/*.msi

      - name: Archive NSIS installer
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}-nsis
          path: claudia/src-tauri/target/${{ matrix.target }}/release/bundle/nsis/*.exe
